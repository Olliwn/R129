# AOK912 Engineering Diary -- August 2026

**Vehicle:** 1991 Mercedes-Benz 500 SL (R129) | VIN: WDB 129066 1F 044414

---

### August 9, 2026 (Sunday) — Touchscreen found disconnected and restored (root cause of "unresponsive UI"); blueman popups disabled; pending touch-menu build deployed

First head-unit session since May 27. Started as "there's a popup I can't bypass", turned into two genuinely separate problems plus a deploy that had been sitting undelivered since May.

**Headline: the UI was unresponsive because the touchscreen was not connected to the Pi at all** — a connection fault in the interim display mount, fixed the same day by re-seating the adapters. The popups were real and worth removing, but they were a red herring — see the correction section below.

#### Symptom 1 — "active pop up window that I do not manage to bypass"

Car parked outside with the Pi powered. Pi reachable at `192.168.1.36` via `r129.local`, uptime 3 min (fresh boot). A `grim` screenshot over SSH showed popups immediately: **two stacked `blueman-applet` notification windows** ("iPhone / Disconnected") floating in the middle of the display, over the R129 wireframe. `wlrctl window list` confirmed three windows — the R129 UI plus two `blueman-applet: iPhone`.

These are desktop Bluetooth notifications from the Raspberry Pi OS BT applet, not anything in the R129 UI. They float above the Qt surface and take Wayland focus, so they *were* worth eliminating on a head unit. The UI process itself was healthy the whole time (`r129-ui.service` active, PID 1455 alive, wireframe still animating).

#### Fix — kill the applet, disable it permanently

`blueman-applet` is **redundant on this system**: Bluetooth pairing is already handled headlessly by `~/.config/systemd/user/bt-agent.service` (`bt-agent --capability=NoInputNoOutput`, active + enabled). The applet contributes nothing but popup noise on a head unit with no desktop.

1. Killed `blueman-applet` (PID 1177) and `blueman-tray` (PID 1347).
2. Disabled autostart with a user-level override shadowing `/etc/xdg/autostart/blueman.desktop`:

```ini
# ~/.config/autostart/blueman.desktop
[Desktop Entry]
Type=Application
Name=blueman-applet
Exec=/usr/bin/blueman-applet
Hidden=true
X-R129-Note=Disabled for the car head unit: blueman popup notifications float
 over the R129 UI and steal touch focus. Bluetooth pairing is handled
 headlessly by bt-agent.service (NoInputNoOutput), so the applet is redundant.
```

`Hidden=true` in the user autostart directory suppresses the system-wide entry without touching `/etc`, so an OS package update won't silently re-enable it and we haven't diverged from the distro's files.

Post-fix `wlrctl window list` shows only `python3: R129 Driver UI`. Screen clean.

#### Symptom 2 (the actual root cause) — touchscreen never enumerated

**Correction to the first diagnosis this session.** With the popups gone the screen was still completely unresponsive — no menu navigation, no drag on the Home wireframe. The popups had been assumed to be the cause of the unresponsiveness; they were not. They were a coincident, separate annoyance.

Real cause, established in three checks:

| Check | Result |
| :--- | :--- |
| `sudo dmesg \| grep -i 0712` | **`NEVER ENUMERATED`** — the Waveshare touch controller (`0712:000a`) has not appeared once since boot |
| `/proc/bus/input/devices` | Only `pwr_button`, `vc4-hdmi-0/1` (CEC), the two HDMI-jack switches, and a virtual `iPhone (AVRCP)` node. **No touch device at all.** |
| `lsusb` / port walk | Only `2e4f:4002` (Audiotec Fischer DSP) on `3-1` and `1314:1520` (Carlinkit) on `3-2`. Both remaining Pi USB ports empty. |

The compositor has no touch device to read, so no amount of software work can help. Software side confirmed intact and *not* the problem: `autotouch` still installed (`ii autotouch 0.1`), kanshi rotation profile unchanged (`transform 90`), UI service healthy.

**Key discriminator: the panel is lit and rendering, but touch is dead.** Per `work/display_mount/README.md` the panel exposes *two* micro-USB ports — one **touch + power**, one **power-only** (documented as unused) — plus 180° adapters redirecting connectors rearward. A lit panel with dead touch means power is arriving while the USB data lines are not working. Three candidates, most likely first after a ~10-week layoff in a car:

1. Cable is in the **power-only** micro-USB port instead of the touch/power port.
2. Micro-USB connector or its 180° adapter has vibrated **partially loose** — outer power pins still contact, inner data pins do not. Very common failure mode for micro-USB under vibration.
3. **Cable failure or a charge-only cable.** Step 6 of the bring-up plan already records that *"the first two USB cables tried were charge-only (no data lines); the third cable worked"* — this trap has bitten this exact build once before.

**Diagnostic caveat worth remembering:** `grim` captures the *compositor framebuffer*, not the physical panel. A clean screenshot proves the Pi is rendering; it says nothing about whether the panel is lit or the touch digitiser is alive. Owner confirmed visually that the panel is lit.

Resolution needed physical access, so a live `udevadm monitor` was left running to catch the moment it enumerated — see the resolution section below.

#### Resolution — re-seating the adapters restored touch

Owner re-seated the cable, and part-way through the process the digitiser appeared: `0712:000a` enumerated at full speed on port **`1-2`** (bus 1, which had been completely empty), `hid-multitouch` bound, and `event6` + `mouse0` registered. Serial `20231224X8`.

Then, 61 seconds later, `usb 1-2: USB disconnect` — it dropped again with nobody touching it. Owner then **reconnected all the adapters and the display, after which both display and touch worked**, and it has been stable since. So the fault was never a failed component: it is the connection through the interim adapter chain, which is expected to be flaky until the proper display-to-cubby-frame adapter is manufactured (the panel is currently on a temporary mount). Downgraded to a MEDIUM open issue against the display-mount work rather than a HIGH head-unit fault.

Verified-good baseline captured while the link was up — useful next time this looks dead:

| Layer | State |
| :--- | :--- |
| Device | `0712:000a` "Waveshare Waveshare", full-speed, port `1-2` |
| Driver | `hid-multitouch` bound; nodes `event6` + `mouse0` |
| libinput | `/dev/input/event6`, `Capabilities: touch`, seat0 |
| Mapping | `rc.xml`: `<touch deviceName="Waveshare  Waveshare " mapToOutput="HDMI-A-1" mouseEmulation="yes"/>` |
| Output | `HDMI-A-1` 1080x1920 @ 60.555 Hz, `Transform: 90` |

Two findings that cost time and are worth pinning:

- **The touch→output mapping was already correct and static**, declared in `~/.config/labwc/rc.xml` and matched on the exact device name (note the *double* spaces in `"Waveshare  Waveshare "`). No rotation fix-up was needed despite the device appearing long after the compositor started, and `mouseEmulation="yes"` is what lets touch drive the Qt `mousePressEvent` handlers the May touch refactor added. `autotouch` ships only an autostart `.desktop` and a binary — the persistent mapping is the labwc config, so that is the file to check if touch ever lands rotated or offset.
- **Two benign signals that look like faults.** The kernel logs `usb 1-2: config 1 has an invalid interface number: 1 but max is 0` and `config 1 has no interface number 0` on *every* enumeration of this panel — a Waveshare descriptor quirk; `hid-multitouch` binds regardless. And `libinput debug-events --device /dev/input/event6` fails with `client bug: Invalid path`, which reads like a tool or permissions problem but simply means the node no longer exists — i.e. it is itself a symptom of the disconnect, and the honest check is `ls /dev/input/` first.

Useful diagnostics for next time: `sudo dmesg | grep -iE "0712|usb 1-2"` for the full enumerate/disconnect history with timestamps, and `sudo dmesg -W | grep --line-buffered -iE "0712|Waveshare|disconnect"` to live-watch. Use `dmesg -W` and not `-w` — `-w` replays the whole existing buffer first, which produced a stream of false "it just enumerated!" alarms this session while the device was in fact long gone.

##### Gotcha repeated: pgrep/pkill self-match killed the SSH session (again)

Same trap as the May 27 session, in a new costume. The kill command used:

```bash
PIDS=$(pgrep -f "blueman-applet|blueman-tray")
```

`pgrep -f` matches against full command lines — including the command line of the `bash -c` wrapper that SSH spawned, which literally contains the string `blueman-applet|blueman-tray`. So the pattern matched its own shell (PIDs 3175/3176 appeared in the kill list alongside the two real targets) and the session died with exit 255. The applet did get killed, but the connection dropped mid-command.

**Standing workaround for this repo:** wrap one character of the pattern in a single-character class so the regex still matches the target but the literal command line does not match the regex:

```bash
pgrep -af "blueman-[a]pplet|blueman-[t]ray"
```

`blueman-[a]pplet` matches the string `blueman-applet`, but the command line contains `blueman-[a]pplet`, which the regex does not match. Used for every subsequent process query this session with no further self-kills. Worth using by default on this Pi given how often we grep for processes over SSH.

#### Deployed the pending touch-menu build

The May 27 commit `ab16470` had the touch-operable menu work committed but never rsynced to the Pi (the deploy attempt hung because the car had already been driven out of Wi-Fi range). Pushed it now — 7 files: `diag_view.py`, `exit_view.py`, `main_window.py`, `settings_view.py`, `sidebar.py`, `split_pane_view.py`, `theme.py`.

Deploy procedure used (worth keeping as the standard):

1. `rsync -avzn` **dry run first** to confirm the file list matches expectation.
2. `rsync -avz` for the real transfer.
3. **`python3 -m py_compile *.py` on the Pi before restarting the service** — catches a syntax error while the old UI is still running, instead of discovering it after the restart leaves a dead head unit in a car that may be out of network range. Cheap insurance; adopt as standard.
4. `systemctl --user restart r129-ui.service`.

All files compiled clean; service restarted to PID 3546.

#### Verification

Remote-drove the UI with `wtype` (Down ×4 + Return) to reach Settings, screenshotted, then returned to Home (Left + Up ×4 + Return):

- **Settings renders correctly** with the new touch layer — categories in the left pane (DISPLAY selected), options in the right pane. Confirms the new `_register_detail_item` / `_register_param_bar` hit-rect registration calls run inside `_draw_detail` without error, which was the main regression risk of the touch refactor.
- **Sidebar polarity inversion is live** (amber-filled slots, dark icon dots, red selection pip tracking the active page). That part had been deployed back in May.
- Screen clean on Home, no stray windows.

Actual finger testing of the new tap targets (category tap, option tap, slider tap-to-set and drag-to-scrub, 400 ms hold-to-quit on Exit) still needs the owner at the car — a remote `wtype` keystroke exercises the paint path but not the `mousePressEvent` handlers.

**Careful with `wtype` on this UI:** `Escape` and `Q` are wired to `MainWindow.keyPressEvent` → `close()`, which quits the app. Only send navigation keys (arrows / Return) when driving the UI remotely.

#### Open follow-ups

- **~~PRIMARY: reconnect the touchscreen.~~ Done** — re-seating the adapters restored it; display and touch both working. Residual mechanical unreliability moved to the display-mount work below.
- **Manufacture the proper display-to-cubby-frame adapter** (`work/display_mount/README.md`). The panel is on an interim mount whose USB tail has no strain relief, which is what produced today's 61-second dropout. This is now the fix for touch reliability, not any electrical work.
- **Finger-test the touch layer** at the car, now unblocked: Settings category/option taps, slider tap + drag, Exit hold-to-quit progress bar, and confirm the 400 ms hold threshold feels right (it was lowered from 600 ms specifically because a finger covers the visual feedback).
- **Consider a physical input fallback.** This session made it obvious that with the touchscreen down there is *no* way to drive the UI in the car — the rotary encoder is still not installed, so the head unit had zero usable input. That raises the priority of the Alps RKJXT1F42001 rotary/joystick install (`input_manager.py` already supports it on GPIO 17/27/22/23/24/25/5); it would have kept the car usable through exactly this failure.
- **CarPlay not re-tested this session** — LIVI was not running and the iPhone shows `Connected: no` (paired + bonded + trusted). Next in-car session should confirm the May 27 re-pair procedure still holds after the layoff.
- **~~`carplay_view.py` dongle-detection fast-path hardcoded to `/sys/bus/usb/devices/3-1`.~~ Fixed** — and it was worse than the cosmetic bug assumed in May. `3-1` is now the Audiotec Fischer DSP, so the `open()` *succeeded* and returned vendor `2e4f`, setting `_dongle_present = False`; because no `OSError` was raised the `lsusb` fallback never ran, so the view reported **"DONGLE NOT FOUND" with the dongle plugged in**. Replaced with `_scan_sysfs()`, which walks `/sys/bus/usb/devices` matching VID *and* PID (`1314:1520`) on any port, and returns `None` only when sysfs itself is unreadable so the `lsusb` subprocess is still avoided on the 5-second poll path.
- **Runtime-adjustable amber brightness** (Qt-level scalar) still open, carried over from May.

#### Files updated this entry

- `~/.config/autostart/blueman.desktop` **on the Pi** (not in this repo) — new user-level autostart override.
- `docs/RPi5_Bring-up_Plan.md` — new "Desktop popup suppression" section, a touchscreen-dead triage recipe under Step 6, and the `py_compile`-before-restart deploy recipe.
- `docs/known_issues.md` — disconnected-touchscreen entry moved to RESOLVED with the verified-good stack baseline; new MEDIUM OPEN entry for the interim-mount dropout.
- `UI_rpi5/src/carplay_view.py` — dongle detection now scans by VID/PID instead of the hardcoded `3-1` port.
- `docs/diary/2026-08.md` — this entry (first August entry; diary had stopped at May 27).
