# AOK912 Engineering Diary -- August 2026

**Vehicle:** 1991 Mercedes-Benz 500 SL (R129) | VIN: WDB 129066 1F 044414

---

### August 9, 2026 (Sunday) — Head unit unblocked: blueman popups disabled, pending touch-menu build deployed

First head-unit session since May 27. Two items: a blocking popup on the car display, and the touch-operable menu work that was committed in May but never pushed to the Pi.

#### Symptom — "active pop up window that I do not manage to bypass"

Car parked outside with the Pi powered. Pi reachable at `192.168.1.36` via `r129.local`, uptime 3 min (fresh boot). A `grim` screenshot over SSH showed the cause immediately: **two stacked `blueman-applet` notification windows** ("iPhone / Disconnected") floating in the middle of the display, over the R129 wireframe. `wlrctl window list` confirmed three windows — the R129 UI plus two `blueman-applet: iPhone`.

These are desktop Bluetooth notifications from the Raspberry Pi OS BT applet, not anything in the R129 UI. They float above the Qt surface, take Wayland focus, and intercept touch in the centre of the screen — which reads to the user as "the UI has hung", even though the UI process was healthy the whole time (`r129-ui.service` active, PID 1455 alive, wireframe still animating).

#### Fix — kill the applet, disable it permanently

`blueman-applet` is **redundant on this system**: Bluetooth pairing is already handled headlessly by `~/.config/systemd/user/bt-agent.service` (`bt-agent --capability=NoInputNoOutput`, active + enabled). The applet contributes nothing but popup noise on a head unit with no desktop.

1. Killed `blueman-applet` (PID 1177) and `blueman-tray` (PID 1347).
2. Disabled autostart with a user-level override shadowing `/etc/xdg/autostart/blueman.desktop`:

```ini
# ~/.config/autostart/blueman.desktop
[Desktop Entry]
Type=Application
Name=blueman-applet
Exec=/usr/bin/blueman-applet
Hidden=true
X-R129-Note=Disabled for the car head unit: blueman popup notifications float
 over the R129 UI and steal touch focus. Bluetooth pairing is handled
 headlessly by bt-agent.service (NoInputNoOutput), so the applet is redundant.
```

`Hidden=true` in the user autostart directory suppresses the system-wide entry without touching `/etc`, so an OS package update won't silently re-enable it and we haven't diverged from the distro's files.

Post-fix `wlrctl window list` shows only `python3: R129 Driver UI`. Screen clean.

##### Gotcha repeated: pgrep/pkill self-match killed the SSH session (again)

Same trap as the May 27 session, in a new costume. The kill command used:

```bash
PIDS=$(pgrep -f "blueman-applet|blueman-tray")
```

`pgrep -f` matches against full command lines — including the command line of the `bash -c` wrapper that SSH spawned, which literally contains the string `blueman-applet|blueman-tray`. So the pattern matched its own shell (PIDs 3175/3176 appeared in the kill list alongside the two real targets) and the session died with exit 255. The applet did get killed, but the connection dropped mid-command.

**Standing workaround for this repo:** wrap one character of the pattern in a single-character class so the regex still matches the target but the literal command line does not match the regex:

```bash
pgrep -af "blueman-[a]pplet|blueman-[t]ray"
```

`blueman-[a]pplet` matches the string `blueman-applet`, but the command line contains `blueman-[a]pplet`, which the regex does not match. Used for every subsequent process query this session with no further self-kills. Worth using by default on this Pi given how often we grep for processes over SSH.

#### Deployed the pending touch-menu build

The May 27 commit `ab16470` had the touch-operable menu work committed but never rsynced to the Pi (the deploy attempt hung because the car had already been driven out of Wi-Fi range). Pushed it now — 7 files: `diag_view.py`, `exit_view.py`, `main_window.py`, `settings_view.py`, `sidebar.py`, `split_pane_view.py`, `theme.py`.

Deploy procedure used (worth keeping as the standard):

1. `rsync -avzn` **dry run first** to confirm the file list matches expectation.
2. `rsync -avz` for the real transfer.
3. **`python3 -m py_compile *.py` on the Pi before restarting the service** — catches a syntax error while the old UI is still running, instead of discovering it after the restart leaves a dead head unit in a car that may be out of network range. Cheap insurance; adopt as standard.
4. `systemctl --user restart r129-ui.service`.

All files compiled clean; service restarted to PID 3546.

#### Verification

Remote-drove the UI with `wtype` (Down ×4 + Return) to reach Settings, screenshotted, then returned to Home (Left + Up ×4 + Return):

- **Settings renders correctly** with the new touch layer — categories in the left pane (DISPLAY selected), options in the right pane. Confirms the new `_register_detail_item` / `_register_param_bar` hit-rect registration calls run inside `_draw_detail` without error, which was the main regression risk of the touch refactor.
- **Sidebar polarity inversion is live** (amber-filled slots, dark icon dots, red selection pip tracking the active page). That part had been deployed back in May.
- Screen clean on Home, no stray windows.

Actual finger testing of the new tap targets (category tap, option tap, slider tap-to-set and drag-to-scrub, 400 ms hold-to-quit on Exit) still needs the owner at the car — a remote `wtype` keystroke exercises the paint path but not the `mousePressEvent` handlers.

**Careful with `wtype` on this UI:** `Escape` and `Q` are wired to `MainWindow.keyPressEvent` → `close()`, which quits the app. Only send navigation keys (arrows / Return) when driving the UI remotely.

#### Open follow-ups

- **Finger-test the touch layer** at the car: Settings category/option taps, slider tap + drag, Exit hold-to-quit progress bar, and confirm the 400 ms hold threshold feels right (it was lowered from 600 ms specifically because a finger covers the visual feedback).
- **CarPlay not re-tested this session** — LIVI was not running and the iPhone shows `Connected: no` (paired + bonded + trusted). Next in-car session should confirm the May 27 re-pair procedure still holds after the layoff.
- **`carplay_view.py` dongle-detection fast-path** still hardcoded to `/sys/bus/usb/devices/3-1`; dongle has been seen on `3-2`. Carried over from May.
- **Runtime-adjustable amber brightness** (Qt-level scalar) still open, carried over from May.

#### Files updated this entry

- `~/.config/autostart/blueman.desktop` **on the Pi** (not in this repo) — new user-level autostart override.
- `docs/RPi5_Bring-up_Plan.md` — new "Desktop popup suppression" section and the `py_compile`-before-restart deploy recipe.
- `docs/diary/2026-08.md` — this entry (first August entry; diary had stopped at May 27).
